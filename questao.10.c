#include <stdio.h>
#include <time.h>
#include <string.h>

int main()
{
    /*
    Se x for declarado como do tipo char, mostra o string apartir da posição declarada.
    Exemplo: uma string declarada com a palavra "gato", x+1 vai mostrar a palavra ato.
    Se x for declarado como do tipo int, mostra os valores da variável x, os chamados lixos de memória
    e soma com o número seguinte.
    Se x for declarado como do tipo float ou double, mostra as variáveis como do tipo int, já que são somadas
    com variáveis do tipo int.
    */
}
